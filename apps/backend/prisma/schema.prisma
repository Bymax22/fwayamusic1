generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model User {
  id                     Int                     @id @default(autoincrement())
  email                  String                  @unique
  username               String                  @unique
  passwordHash           String                  @map("password_hash")
  displayName            String?                 @map("display_name")
  avatarUrl              String?                 @map("avatar_url")
  isSocialAuth           Boolean                 @default(false) @map("is_social_auth")
  provider               String?
  socialId               String?                 @map("social_id")
  lastLoginAt            DateTime?               @map("last_login_at")
  role                   UserRole                @default(USER)
  isPremium              Boolean                 @default(false) @map("is_premium")
  premiumUntil           DateTime?               @map("premium_until")
  walletBalance          Float                   @default(0) @map("wallet_balance")
  totalEarnings          Float                   @default(0) @map("total_earnings")
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")
  commissionRate         Float?                  @default(0.2) @map("commission_rate")
  country                String?                 @default("US")
  isReseller             Boolean                 @default(false) @map("is_reseller")
  paidCommission         Float                   @default(0) @map("paid_commission")
  resellerCode           String?                 @unique @map("reseller_code")
  totalCommission        Float                   @default(0) @map("total_commission")
  acceptedPrivacy        Boolean                 @default(false) @map("accepted_privacy")
  acceptedTerms          Boolean                 @default(false) @map("accepted_terms")
  address                Json?
  artistName             String?                 @map("artist_name")
  bio                    String?
  businessName           String?                 @map("business_name")
  businessType           String?                 @map("business_type")
  consentDate            DateTime?               @map("consent_date")
  dataSharing            Boolean                 @default(false) @map("data_sharing")
  dateOfBirth            DateTime?               @map("date_of_birth")
  isEmailVerified        Boolean                 @default(false) @map("is_email_verified")
  isPhoneVerified        Boolean                 @default(false) @map("is_phone_verified")
  marketingEmails        Boolean                 @default(false) @map("marketing_emails")
  phoneNumber            String?                 @map("phone_number")
  socialLinks            Json?                   @map("social_links")
  stageName              String?                 @map("stage_name")
  status                 UserStatus              @default(PENDING) @map("status")
  taxId                  String?                 @map("tax_id")
  taxNumber              String?                 @map("tax_number")
  website                String?
  acceptedCookies        Boolean                 @default(false) @map("accepted_cookies")
  accountDeactivatedAt   DateTime?               @map("account_deactivated_at")
  allowExplicit          Boolean                 @default(true) @map("allow_explicit")
  artistBio              String?                 @map("artist_bio")
  autoDownloadQuality    VideoQuality?           @map("auto_download_quality")
  autoRenewSubscription  Boolean                 @default(true) @map("auto_renew_subscription")
  businessLicense        String?                 @map("business_license")
  businessRegistration   String?                 @map("business_registration")
  businessVerified       Boolean                 @default(false) @map("business_verified")
  cloudinaryPublicId     String?                 @map("cloudinary_public_id")
  coverCloudinaryId      String?                 @map("cover_cloudinary_id")
  coverImageUrl          String?                 @map("cover_image_url")
  default_currency       Currency                @default(USD)
  deletionDetails        Json?                   @map("deletion_details")
  deletionReason         String?                 @map("deletion_reason")
  deletionRequestedAt    DateTime?               @map("deletion_requested_at")
  freeTrialExpiresAt     DateTime?               @map("free_trial_expires_at")
  freeTrialUsed          Boolean                 @default(false) @map("free_trial_used")
  gender                 String?
  isAccountVerified      Boolean                 @default(false) @map("is_account_verified")
  isArtist               Boolean                 @default(false) @map("is_artist")
  isProducer             Boolean                 @default(false) @map("is_producer")
  language               String?                 @default("en")
  lastLoginIp            String?                 @map("last_login_ip")
  lastPasswordReset      DateTime?               @map("last_password_reset")
  monthlyEarnings        Float                   @default(0) @map("monthly_earnings")
  monthlySpend           Float                   @default(0) @map("monthly_spend")
  notificationSettings   Json?                   @map("notification_settings")
  passwordChangedAt      DateTime?               @map("password_changed_at")
  producerBio            String?                 @map("producer_bio")
  producerFollowers      Int                     @default(0) @map("producer_followers")
  producerName           String?                 @map("producer_name")
  producerWebsite        String?                 @map("producer_website")
  referralCode           String?                 @unique @map("referral_code")
  resellerEarnings       Float                   @default(0) @map("reseller_earnings")
  streamingQualityPref   StreamingQualityPreset  @default(ADAPTIVE) @map("streaming_quality_pref")
  subscriptionPlan       SubscriptionPlan?       @map("subscription_plan")
  timezone               String?                 @default("UTC")
  totalFollowers         Int                     @default(0) @map("total_followers")
  totalFollowing         Int                     @default(0) @map("total_following")
  totalSpent             Float                   @default(0) @map("total_spent")
  totalStreams           Int                     @default(0) @map("total_streams")
  twoFactorEnabled       Boolean                 @default(false) @map("two_factor_enabled")
  twoFactorSecret        String?                 @map("two_factor_secret")
  verifiedArtist         Boolean                 @default(false) @map("verified_artist")
  verifiedArtistAt       DateTime?               @map("verified_artist_at")
  verifiedProducer       Boolean                 @default(false) @map("verified_producer")
  albums                 Album[]
  auditLogs              AuditLog[]
  commissions            Commission[]
  userContentModerations ContentModeration[]     @relation("ContentCreator")
  contentModerations     ContentModeration[]     @relation("ModerationReviewer")
  deviceLicenses         DeviceLicense[]
  downloads              Download[]
  followers              Follower[]              @relation("followers")
  following              Follower[]              @relation("following")
  reviewedDocuments      KYCDocument[]           @relation("DocumentReviewer")
  kycSubmissions         KYCDocument[]
  libraries              Library[]
  liveViewers            LiveStreamViewer[]
  liveStreams            LiveStream[]
  media                  Media[]
  mediaComments          MediaComment[]
  interactions           MediaInteraction[]
  ratings                MediaRating[]
  news                   News[]
  newsBookmarks          NewsBookmark[]
  newsComments           NewsComment[]
  newsLikes              NewsLike[]
  newsReactions          NewsReaction[]
  newsReports            NewsReport[]
  newsShares             NewsShare[]
  newsViews              NewsView[]
  notifications          Notification[]
  nowPlaying             NowPlaying?
  paymentAccounts        PaymentAccount[]
  payoutRequests         PayoutRequest[]
  playlists              Playlist[]
  collaborations         ProducerCollaboration[] @relation("CollaboratingArtist")
  collaborationRequests  ProducerCollaboration[] @relation("AssignedProducer")
  queuedPayouts          QueuedPayout[]
  resellerLinks          ResellerLink[]
  savedForLater          SavedForLater[]
  searchHistory          SearchHistory[]
  sessions               Session[]
  subscriptions          Subscription[]
  transactions           Transaction[]
  devices                UserDevice[]
  recommendations        UserRecommendation[]
  verifications          Verification[]
  watchHistory           WatchHistory[]
  beatPacks              BeatPack[]          @relation("BeatPacks")
  resources              Resource[]          @relation("Resources")

  @@index([email])
  @@index([username])
  @@index([isArtist])
  @@index([isProducer])
  @@index([isReseller])
  @@index([resellerCode])
  @@index([status])
  @@index([isPremium])
  @@index([verifiedArtist])
  @@index([createdAt])
  @@map("users")
}

model Media {
  id                     Int                     @id @default(autoincrement())
  url                    String                  @unique
  artCoverUrl            String?                 @map("art_cover_url")
  thumbnailUrl           String?                 @map("thumbnail_url")
  format                 String?
  duration               Int?
  title                  String
  description            String?
  genre                  String?
  tags                   String[]
  type                   MediaType               @default(AUDIO)
  accessType             MediaAccessType         @default(FREE) @map("access_type")
  price                  Float?                  @default(0)
  isExplicit             Boolean                 @default(false) @map("is_explicit")
  playCount              Int                     @default(0) @map("play_count")
  downloadCount          Int                     @default(0) @map("download_count")
  shareCount             Int                     @default(0) @map("share_count")
  lastPlayedAt           DateTime?               @map("last_played_at")
  createdAt              DateTime                @default(now()) @map("created_at")
  updatedAt              DateTime                @updatedAt @map("updated_at")
  userId                 Int?                    @map("user_id")
  cloudinaryPublicId     String?                 @map("cloudinary_public_id")
  bpm                    Int?
  key                    String?
  energy                 Float?
  danceability           Float?
  valence                Float?
  acousticness           Float?
  allowReselling         Boolean                 @default(true) @map("allow_reselling")
  artistCommissionRate   Float                   @default(0.5) @map("artist_commission_rate")
  encryptionKey          String?                 @map("encryption_key")
  isDRMProtected         Boolean                 @default(false) @map("is_drm_protected")
  maxDevices             Int                     @default(1) @map("max_devices")
  platformCommissionRate Float                   @default(0.3) @map("platform_commission_rate")
  resellerCommissionRate Float?                  @map("reseller_commission_rate")
  albumId                Int?                    @map("album_id")
  allowCommercialUse     Boolean                 @default(true) @map("allow_commercial_use")
  allowDerivatives       Boolean                 @default(true) @map("allow_derivatives")
  audioChannels          Int?                    @map("audio_channels")
  audioCodec             AudioCodec?             @map("audio_codec")
  audioFeatureCategory   AudioFeatureCategory?   @map("audio_feature_category")
  bitrate                Int?
  composer               String?
  contentStatus          ContentStatus           @default(DRAFT) @map("content_status")
  copyrightOwner         String?                 @map("copyright_owner")
  copyrightYear          Int?                    @map("copyright_year")
  deletedAt              DateTime?               @map("deleted_at")
  deletionDetails        Json?                   @map("deletion_details")
  deletionReason         DeletionReason?         @map("deletion_reason")
  fileSize               BigInt?                 @map("file_size")
  frameRate              Float?                  @map("frame_rate")
  height                 Int?
  isrc                   String?                 @map("isrc")
  language               String?
  licenseType            String?                 @map("license_type")
  likeCount              Int                     @default(0) @map("like_count")
  lyricist               String?
  mood                   String?
  originalMediaId        Int?                    @map("original_media_id")
  releaseDate            DateTime?               @map("release_date")
  rights                 Json?
  sampleRate             Int?                    @map("sample_rate")
  thumbnailCloudinaryId  String?                 @map("thumbnail_cloudinary_id")
  totalStreamingTime     BigInt                  @default(0) @map("total_streaming_time")
  version                Int                     @default(1)
  videoCodec             VideoCodec?             @map("video_codec")
  width                  Int?
  commissions            Commission[]
  contentModerations     ContentModeration[]
  deviceLicenses         DeviceLicense[]
  downloads              Download[]
  libraryItems           LibraryItem[]
  liveStreamSessions     LiveStreamSession[]
  album                  Album?                  @relation(fields: [albumId], references: [id])
  originalMedia          Media?                  @relation("MediaVersions", fields: [originalMediaId], references: [id])
  versionedMedia         Media[]                 @relation("MediaVersions")
  user                   User?                   @relation(fields: [userId], references: [id], onDelete: Cascade)
  categories             MediaCategory[]
  comments               MediaComment[]
  interactions           MediaInteraction[]
  ratings                MediaRating[]
  nowPlaying             NowPlaying[]
  playlistEntries        PlaylistEntry[]
  collaborators          ProducerCollaboration[]
  resellerLinks          ResellerLink[]
  savedForLater          SavedForLater[]
  beatPackBeats         BeatPackBeat[]        @relation("BeatPackBeats")
  transactions           Transaction[]
  recommendations        UserRecommendation[]
  videoQualityVariants   VideoVariant[]
  watchHistory           WatchHistory[]

  @@index([userId])
  @@index([accessType])
  @@index([allowReselling])
  @@index([contentStatus])
  @@index([type])
  @@index([deletedAt])
  @@index([createdAt])
  @@index([genre])
  @@index([playCount])
  @@map("media")
}

model VideoVariant {
  id        Int          @id @default(autoincrement())
  mediaId   Int          @map("media_id")
  quality   VideoQuality
  url       String
  fileSize  BigInt       @map("file_size")
  bitrate   Int
  width     Int
  height    Int
  codec     VideoCodec
  createdAt DateTime     @default(now()) @map("created_at")
  media     Media        @relation(fields: [mediaId], references: [id], onDelete: Cascade)

  @@unique([mediaId, quality])
  @@map("video_variants")
}

model Album {
  id                 Int           @id @default(autoincrement())
  title              String
  description        String?
  userId             Int           @map("user_id")
  coverUrl           String?       @map("cover_url")
  cloudinaryPublicId String?       @map("cloudinary_public_id")
  releaseDate        DateTime?     @map("release_date")
  type               String?
  recordLabel        String?       @map("record_label")
  credits            Json?
  contentStatus      ContentStatus @default(DRAFT) @map("content_status")
  isPublic           Boolean       @default(false) @map("is_public")
  playCount          Int           @default(0) @map("play_count")
  downloadCount      Int           @default(0) @map("download_count")
  createdAt          DateTime      @default(now()) @map("created_at")
  updatedAt          DateTime      @updatedAt @map("updated_at")
  user               User          @relation(fields: [userId], references: [id], onDelete: Cascade)
  media              Media[]

  @@index([userId])
  @@index([releaseDate])
  @@map("albums")
}

model MediaRating {
  id        Int         @id @default(autoincrement())
  mediaId   Int         @map("media_id")
  userId    Int         @map("user_id")
  rating    RatingValue
  comment   String?
  createdAt DateTime    @default(now()) @map("created_at")
  updatedAt DateTime    @updatedAt @map("updated_at")
  media     Media       @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  user      User        @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([mediaId, userId])
  @@map("media_ratings")
}

model WatchHistory {
  id                Int           @id @default(autoincrement())
  userId            Int           @map("user_id")
  mediaId           Int           @map("media_id")
  watchedAt         DateTime      @default(now()) @map("watched_at")
  watchDuration     Int?          @map("watch_duration")
  completionPercent Float?        @map("completion_percent")
  quality           VideoQuality?
  deviceType        String?       @map("device_type")
  isOfflineWatch    Boolean       @default(false) @map("is_offline_watch")
  media             Media         @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  user              User          @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, mediaId])
  @@index([userId, watchedAt])
  @@index([mediaId])
  @@map("watch_history")
}

model SavedForLater {
  id       Int      @id @default(autoincrement())
  userId   Int      @map("user_id")
  mediaId  Int      @map("media_id")
  savedAt  DateTime @default(now()) @map("saved_at")
  priority Int      @default(0)
  notes    String?
  media    Media    @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, mediaId])
  @@map("saved_for_later")
}

model MediaCategory {
  mediaId    Int      @map("media_id")
  categoryId Int      @map("category_id")
  assignedAt DateTime @default(now()) @map("assigned_at")
  category   Category @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  media      Media    @relation(fields: [mediaId], references: [id], onDelete: Cascade)

  @@id([mediaId, categoryId])
  @@map("media_categories")
}

model Category {
  id          Int             @id @default(autoincrement())
  name        String          @unique
  description String?
  slug        String          @unique
  imageUrl    String?         @map("image_url")
  isActive    Boolean         @default(true) @map("is_active")
  order       Int             @default(0)
  media       MediaCategory[]

  @@map("categories")
}

model MediaComment {
  id        Int            @id @default(autoincrement())
  mediaId   Int            @map("media_id")
  userId    Int            @map("user_id")
  parentId  Int?           @map("parent_id")
  content   String
  likes     Int            @default(0)
  createdAt DateTime       @default(now()) @map("created_at")
  updatedAt DateTime       @updatedAt @map("updated_at")
  deletedAt DateTime?      @map("deleted_at")
  editedAt  DateTime?      @map("edited_at")
  isEdited  Boolean        @default(false) @map("is_edited")
  isPinned  Boolean        @default(false) @map("is_pinned")
  media     Media          @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  parent    MediaComment?  @relation("CommentReplies", fields: [parentId], references: [id])
  replies   MediaComment[] @relation("CommentReplies")
  user      User           @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([mediaId])
  @@index([userId])
  @@index([parentId])
  @@map("media_comments")
}

model MediaInteraction {
  id                 Int       @id @default(autoincrement())
  mediaId            Int       @map("media_id")
  userId             Int?      @map("user_id")
  liked              Boolean?  @default(false)
  saved              Boolean?  @default(false)
  played             Boolean?  @default(false)
  position           Int?
  interactedAt       DateTime? @default(now()) @map("interacted_at")
  lastPlayedPosition Int?      @map("last_played_position")
  media              Media     @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  user               User?     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([mediaId, userId])
  @@index([userId])
  @@map("media_interactions")
}

model Playlist {
  id              Int             @id @default(autoincrement())
  name            String
  description     String?
  coverUrl        String?         @map("cover_url")
  isPublic        Boolean         @default(false) @map("is_public")
  type            PlaylistType    @default(USER)
  rules           Json?
  createdAt       DateTime        @default(now()) @map("created_at")
  updatedAt       DateTime        @updatedAt @map("updated_at")
  userId          Int             @map("user_id")
  isCollaborative Boolean         @default(false) @map("is_collaborative")
  entries         PlaylistEntry[]
  user            User            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([isPublic])
  @@map("playlists")
}

model PlaylistEntry {
  id         Int      @id @default(autoincrement())
  playlistId Int      @map("playlist_id")
  mediaId    Int      @map("media_id")
  position   Int
  addedAt    DateTime @default(now()) @map("added_at")
  addedBy    Int?     @map("added_by")
  media      Media    @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  playlist   Playlist @relation(fields: [playlistId], references: [id], onDelete: Cascade)

  @@unique([playlistId, mediaId])
  @@map("playlist_entries")
}

model NowPlaying {
  id        Int      @id @default(autoincrement())
  userId    Int      @unique @map("user_id")
  mediaId   Int      @map("media_id")
  position  Int      @default(0)
  isPlaying Boolean  @default(false) @map("is_playing")
  deviceId  String?  @map("device_id")
  updatedAt DateTime @updatedAt @map("updated_at")
  media     Media    @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("now_playing")
}

model SearchHistory {
  id              Int      @id @default(autoincrement())
  userId          Int      @map("user_id")
  query           String
  results         Int?
  clickedResultId Int?     @map("clicked_result_id")
  searchedAt      DateTime @default(now()) @map("searched_at")
  user            User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, searchedAt])
  @@map("search_history")
}

model UserRecommendation {
  id          Int                @id @default(autoincrement())
  userId      Int                @map("user_id")
  mediaId     Int                @map("media_id")
  type        RecommendationType
  score       Float?
  reason      String?
  displayedAt DateTime?          @map("displayed_at")
  clickedAt   DateTime?          @map("clicked_at")
  createdAt   DateTime           @default(now()) @map("created_at")
  media       Media              @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  user        User               @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, type])
  @@index([mediaId])
  @@map("user_recommendations")
}

model UserDevice {
  id              Int             @id @default(autoincrement())
  userId          Int?            @map("user_id")
  deviceId        String          @map("device_id")
  deviceName      String          @map("device_name")
  lastActiveAt    DateTime?       @map("last_active_at")
  deviceType      String?         @map("device_type")
  fingerprint     String?         @unique
  os              String?
  appVersion      String?         @map("app_version")
  browserAgent    String?         @map("browser_agent")
  ipAddress       String?         @map("ip_address")
  isCurrentDevice Boolean         @default(false) @map("is_current_device")
  isTrusted       Boolean         @default(false) @map("is_trusted")
  osVersion       String?         @map("os_version")
  licenses        DeviceLicense[]
  user            User?           @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, deviceId])
  @@index([userId])
  @@map("user_devices")
}

model Session {
  id               Int       @id @default(autoincrement())
  userId           Int?      @map("user_id")
  token            String    @unique
  ipAddress        String?   @map("ip_address")
  userAgent        String?   @map("user_agent")
  expiresAt        DateTime  @map("expires_at")
  createdAt        DateTime  @default(now()) @map("created_at")
  refreshExpiresAt DateTime? @map("refresh_expires_at")
  refreshToken     String?   @unique @map("refresh_token")
  revokedAt        DateTime? @map("revoked_at")
  user             User?     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([token])
  @@map("sessions")
}

model Follower {
  id          Int      @id @default(autoincrement())
  followerId  Int      @map("follower_id")
  followingId Int?     @map("following_id")
  createdAt   DateTime @default(now()) @map("created_at")
  follower    User     @relation("followers", fields: [followerId], references: [id], onDelete: Cascade)
  following   User?    @relation("following", fields: [followingId], references: [id], onDelete: Cascade)

  @@unique([followerId, followingId])
  @@index([followerId])
  @@index([followingId])
  @@map("followers")
}

model Download {
  id               Int                @id @default(autoincrement())
  mediaId          Int                @map("media_id")
  userId           Int?               @map("user_id")
  deviceId         String?            @map("device_id")
  downloadedAt     DateTime           @default(now()) @map("downloaded_at")
  expiresAt        DateTime?          @map("expires_at")
  accessType       DownloadAccessType @default(OFFLINE) @map("access_type")
  isDRMProtected   Boolean            @default(false) @map("is_drm_protected")
  licenseKey       String?            @unique @map("license_key")
  extraData        Json?              @map("extra_data")
  downloadProgress Float?             @map("download_progress")
  fileSize         BigInt?            @map("file_size")
  quality          VideoQuality?
  media            Media              @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  user             User?              @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([deviceId])
  @@map("downloads")
}

model Transaction {
  id                 Int                 @id @default(autoincrement())
  amount             Float
  status             TransactionStatus   @default(PENDING)
  userId             Int?                @map("user_id")
  mediaId            Int?                @map("media_id")
  reference          String              @unique
  metadata           Json?
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  currency           Currency            @default(USD)
  exchangeRate       Float?              @map("exchange_rate")
  isResellerSale     Boolean             @default(false) @map("is_reseller_sale")
  originalAmount     Float?              @map("original_amount")
  originalCurrency   Currency?           @map("original_currency")
  paymentMethod      PaymentMethod       @map("payment_method")
  paymentProvider    PaymentProvider     @map("payment_provider")
  providerReference  String?             @map("provider_reference")
  resellerLinkId     Int?                @map("reseller_link_id")
  processedAt        DateTime?           @map("processed_at")
  receipt            String?
  receiptUrl         String?             @map("receipt_url")
  commissions        Commission[]
  deviceLicenses     DeviceLicense[]
  libraryItems       LibraryItem[]
  payoutTransactions PayoutTransaction[]
  queuedPayouts      QueuedPayout[]
  media              Media?              @relation(fields: [mediaId], references: [id])
  resellerLink       ResellerLink?       @relation(fields: [resellerLinkId], references: [id])
  user               User?               @relation(fields: [userId], references: [id])

  @@index([userId, status])
  @@index([paymentProvider, providerReference])
  @@index([resellerLinkId])
  @@index([createdAt])
  @@map("transactions")
}

model Commission {
  id                  Int                @id @default(autoincrement())
  amount              Float
  resellerId          Int                @map("reseller_id")
  transactionId       Int                @map("transaction_id")
  mediaId             Int                @map("media_id")
  isPaid              Boolean            @default(false) @map("is_paid")
  paidAt              DateTime?          @map("paid_at")
  createdAt           DateTime           @default(now()) @map("created_at")
  commissionRate      Float              @map("commission_rate")
  currency            Currency           @default(USD)
  payoutTransactionId Int?               @map("payout_transaction_id")
  status              CommissionStatus   @default(PENDING)
  media               Media              @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  payoutTransaction   PayoutTransaction? @relation(fields: [payoutTransactionId], references: [id])
  reseller            User               @relation(fields: [resellerId], references: [id], onDelete: Cascade)
  transaction         Transaction        @relation(fields: [transactionId], references: [id], onDelete: Cascade)

  @@index([resellerId, status])
  @@index([transactionId])
  @@index([status])
  @@map("commissions")
}

model ResellerLink {
  id                   Int                @id @default(autoincrement())
  code                 String             @unique
  resellerId           Int                @map("reseller_id")
  mediaId              Int                @map("media_id")
  clickCount           Int                @default(0) @map("click_count")
  createdAt            DateTime           @default(now()) @map("created_at")
  conversionCount      Int                @default(0) @map("conversion_count")
  customCommissionRate Float?             @map("custom_commission_rate")
  expiresAt            DateTime?          @map("expires_at")
  status               ResellerLinkStatus @default(ACTIVE)
  earnings             Float              @default(0)
  media                Media              @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  reseller             User               @relation(fields: [resellerId], references: [id], onDelete: Cascade)
  transactions         Transaction[]

  @@unique([resellerId, mediaId])
  @@index([code])
  @@index([status])
  @@map("reseller_links")
}

model Library {
  id          Int           @id @default(autoincrement())
  userId      Int?          @map("user_id")
  name        String
  description String?
  isPrivate   Boolean       @default(true) @map("is_private")
  createdAt   DateTime      @default(now()) @map("created_at")
  updatedAt   DateTime      @updatedAt @map("updated_at")
  order       Int           @default(0)
  user        User?         @relation(fields: [userId], references: [id], onDelete: Cascade)
  items       LibraryItem[]

  @@index([userId])
  @@map("libraries")
}

model LibraryItem {
  id            Int          @id @default(autoincrement())
  libraryId     Int          @map("library_id")
  mediaId       Int          @map("media_id")
  addedAt       DateTime     @default(now()) @map("added_at")
  purchasedAt   DateTime?    @map("purchased_at")
  transactionId Int?         @map("transaction_id")
  library       Library      @relation(fields: [libraryId], references: [id], onDelete: Cascade)
  media         Media        @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  transaction   Transaction? @relation(fields: [transactionId], references: [id])

  @@unique([libraryId, mediaId])
  @@map("library_items")
}

model DeviceLicense {
  id               Int                    @id @default(autoincrement())
  userId           Int                    @map("user_id")
  deviceId         Int                    @map("device_id")
  mediaId          Int                    @map("media_id")
  transactionId    Int?                   @map("transaction_id")
  licenseKey       String                 @unique @map("license_key")
  restrictionLevel DeviceRestrictionLevel @default(STRICT) @map("restriction_level")
  expiresAt        DateTime?              @map("expires_at")
  isActive         Boolean                @default(true) @map("is_active")
  createdAt        DateTime               @default(now()) @map("created_at")
  updatedAt        DateTime               @updatedAt @map("updated_at")
  device           UserDevice             @relation(fields: [deviceId], references: [id], onDelete: Cascade)
  media            Media                  @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  transaction      Transaction?           @relation(fields: [transactionId], references: [id])
  user             User                   @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, deviceId, mediaId])
  @@index([licenseKey])
  @@index([expiresAt])
  @@map("device_licenses")
}

model PaymentAccount {
  id                 Int                 @id @default(autoincrement())
  userId             Int                 @map("user_id")
  provider           PaymentProvider
  accountType        PaymentMethod       @map("account_type")
  accountNumber      String              @map("account_number")
  accountName        String?             @map("account_name")
  country            String?
  currency           Currency            @default(USD)
  isVerified         Boolean             @default(false) @map("is_verified")
  isDefault          Boolean             @default(false) @map("is_default")
  metadata           Json?
  createdAt          DateTime            @default(now()) @map("created_at")
  updatedAt          DateTime            @updatedAt @map("updated_at")
  accountHolder      String?             @map("account_holder")
  verifiedAt         DateTime?           @map("verified_at")
  user               User                @relation(fields: [userId], references: [id], onDelete: Cascade)
  payoutRequests     PayoutRequest[]
  payoutTransactions PayoutTransaction[]

  @@unique([userId, provider, accountNumber])
  @@index([userId, isDefault])
  @@index([isVerified])
  @@map("payment_accounts")
}

model PayoutTransaction {
  id                Int               @id @default(autoincrement())
  paymentAccountId  Int               @map("payment_account_id")
  transactionId     Int?              @map("transaction_id")
  amount            Float
  currency          Currency          @default(USD)
  status            TransactionStatus
  reference         String            @unique
  providerReference String?           @map("provider_reference")
  fees              Float             @default(0)
  metadata          Json?
  processedAt       DateTime?         @map("processed_at")
  createdAt         DateTime          @default(now()) @map("created_at")
  updatedAt         DateTime          @updatedAt @map("updated_at")
  commissions       Commission[]
  paymentAccount    PaymentAccount    @relation(fields: [paymentAccountId], references: [id], onDelete: Cascade)
  transaction       Transaction?      @relation(fields: [transactionId], references: [id])

  @@index([paymentAccountId, status])
  @@index([status])
  @@map("payout_transactions")
}

model PayoutRequest {
  id               Int               @id @default(autoincrement())
  userId           Int               @map("user_id")
  amount           Float
  currency         Currency          @default(USD)
  status           TransactionStatus
  paymentAccountId Int?              @map("payment_account_id")
  reason           String?
  metadata         Json?
  processedAt      DateTime?         @map("processed_at")
  createdAt        DateTime          @default(now()) @map("created_at")
  updatedAt        DateTime          @updatedAt @map("updated_at")
  paymentAccount   PaymentAccount?   @relation(fields: [paymentAccountId], references: [id])
  user             User              @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, status])
  @@index([status])
  @@map("payout_requests")
}

model QueuedPayout {
  id            Int         @id @default(autoincrement())
  userId        Int         @map("user_id")
  transactionId Int         @map("transaction_id")
  amount        Float
  currency      Currency
  type          String
  status        String
  reason        String
  retryCount    Int         @default(0) @map("retry_count")
  lastError     String?     @map("last_error")
  processedAt   DateTime?   @map("processed_at")
  createdAt     DateTime    @default(now()) @map("created_at")
  updatedAt     DateTime    @updatedAt @map("updated_at")
  maxRetries    Int         @default(3) @map("max_retries")
  nextRetryAt   DateTime?   @map("next_retry_at")
  transaction   Transaction @relation(fields: [transactionId], references: [id], onDelete: Cascade)
  user          User        @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([status, createdAt])
  @@index([userId, status])
  @@index([nextRetryAt])
  @@map("queued_payouts")
}

model Subscription {
  id                     Int                @id @default(autoincrement())
  userId                 Int                @map("user_id")
  plan                   SubscriptionPlan
  status                 SubscriptionStatus @default(ACTIVE)
  price                  Float
  currency               Currency           @default(USD)
  provider               PaymentProvider?
  providerSubscriptionId String?            @map("provider_subscription_id")
  autoRenew              Boolean            @default(false) @map("auto_renew")
  startedAt              DateTime           @default(now()) @map("started_at")
  expiresAt              DateTime           @map("expires_at")
  canceledAt             DateTime?          @map("canceled_at")
  metadata               Json?
  createdAt              DateTime           @default(now()) @map("created_at")
  updatedAt              DateTime           @updatedAt @map("updated_at")
  user                   User               @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, status])
  @@index([expiresAt])
  @@map("subscriptions")
}

model Notification {
  id        Int              @id @default(autoincrement())
  userId    Int?             @map("user_id")
  title     String
  message   String
  isRead    Boolean          @default(false) @map("is_read")
  createdAt DateTime         @default(now()) @map("created_at")
  data      Json?
  readAt    DateTime?        @map("read_at")
  type      NotificationType
  user      User?            @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, isRead])
  @@index([userId, createdAt])
  @@map("notifications")
}

model News {
  id                 Int            @id @default(autoincrement())
  title              String
  slug               String         @unique
  summary            String?
  content            String
  updatedAt          DateTime       @updatedAt
  tags               String[]
  metadata           Json?
  authorId           Int?           @map("author_id")
  cloudinaryPublicId String?        @map("cloudinary_public_id")
  imageUrl           String?        @map("image_url")
  isFeatured         Boolean        @default(false) @map("is_featured")
  isPublished        Boolean        @default(true) @map("is_published")
  publishedAt        DateTime       @default(now()) @map("published_at")
  scheduledAt        DateTime?      @map("scheduled_at")
  author             User?          @relation(fields: [authorId], references: [id])
  bookmarks          NewsBookmark[]
  comments           NewsComment[]
  likes              NewsLike[]
  reactions          NewsReaction[]
  reports            NewsReport[]
  shares             NewsShare[]
  views              NewsView[]

  @@index([authorId])
  @@index([publishedAt])
  @@index([isPublished])
  @@map("news")
}

model NewsLike {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now()) @map("created_at")
  newsId    Int      @map("news_id")
  userId    Int      @map("user_id")
  news      News     @relation(fields: [newsId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([newsId, userId])
  @@map("news_likes")
}

model NewsShare {
  id       Int      @id @default(autoincrement())
  newsId   Int      @map("news_id")
  sharedAt DateTime @default(now()) @map("shared_at")
  userId   Int      @map("user_id")
  news     News     @relation(fields: [newsId], references: [id], onDelete: Cascade)
  user     User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("news_shares")
}

model NewsComment {
  id        Int      @id @default(autoincrement())
  content   String
  createdAt DateTime @default(now()) @map("created_at")
  newsId    Int      @map("news_id")
  userId    Int      @map("user_id")
  news      News     @relation(fields: [newsId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("news_comments")
}

model NewsBookmark {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now()) @map("created_at")
  newsId    Int      @map("news_id")
  userId    Int      @map("user_id")
  news      News     @relation(fields: [newsId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([newsId, userId])
  @@map("news_bookmarks")
}

model NewsView {
  id        Int      @id @default(autoincrement())
  ipAddress String?  @map("ip_address")
  newsId    Int      @map("news_id")
  userId    Int?     @map("user_id")
  viewedAt  DateTime @default(now()) @map("viewed_at")
  news      News     @relation(fields: [newsId], references: [id], onDelete: Cascade)
  user      User?    @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("news_views")
}

model NewsReport {
  id         Int      @id @default(autoincrement())
  reason     String
  details    String?
  newsId     Int      @map("news_id")
  reportedAt DateTime @default(now()) @map("reported_at")
  userId     Int      @map("user_id")
  news       News     @relation(fields: [newsId], references: [id], onDelete: Cascade)
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("news_reports")
}

model NewsReaction {
  id        Int      @id @default(autoincrement())
  type      String
  newsId    Int      @map("news_id")
  reactedAt DateTime @default(now()) @map("reacted_at")
  userId    Int      @map("user_id")
  news      News     @relation(fields: [newsId], references: [id], onDelete: Cascade)
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([newsId, userId, type])
  @@map("news_reactions")
}

model KYCDocument {
  id              Int          @id @default(autoincrement())
  userId          Int          @map("user_id")
  documentType    DocumentType @map("document_type")
  documentNumber  String?      @map("document_number")
  frontImageUrl   String       @map("front_image_url")
  backImageUrl    String?      @map("back_image_url")
  selfieImageUrl  String?      @map("selfie_image_url")
  status          KYCStatus    @default(PENDING_REVIEW) @map("status")
  rejectionReason String?      @map("rejection_reason")
  reviewedBy      Int?         @map("reviewed_by")
  reviewedAt      DateTime?    @map("reviewed_at")
  metadata        Json?
  createdAt       DateTime     @default(now()) @map("created_at")
  updatedAt       DateTime     @updatedAt @map("updated_at")
  reviewer        User?        @relation("DocumentReviewer", fields: [reviewedBy], references: [id])
  user            User         @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, status])
  @@index([documentType, status])
  @@map("kyc_documents")
}

model Verification {
  id         Int                @id @default(autoincrement())
  userId     Int?               @map("user_id")
  method     VerificationMethod @map("method")
  code       String?
  token      String?
  isVerified Boolean            @default(false) @map("is_verified")
  expiresAt  DateTime           @map("expires_at")
  verifiedAt DateTime?          @map("verified_at")
  metadata   Json?
  createdAt  DateTime           @default(now()) @map("created_at")
  user       User?              @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([userId, method])
  @@index([token, expiresAt])
  @@map("verifications")
}

model AuditLog {
  id         Int      @id @default(autoincrement())
  userId     Int?     @map("user_id")
  action     String
  resource   String?
  resourceId Int?     @map("resource_id")
  oldValues  Json?    @map("old_values")
  newValues  Json?    @map("new_values")
  ipAddress  String?  @map("ip_address")
  userAgent  String?  @map("user_agent")
  createdAt  DateTime @default(now()) @map("created_at")
  user       User?    @relation(fields: [userId], references: [id])

  @@index([userId, action])
  @@index([resource, resourceId])
  @@index([createdAt])
  @@map("audit_logs")
}

model SystemAlert {
  id         Int       @id @default(autoincrement())
  type       String
  message    String
  severity   String
  resolved   Boolean   @default(false)
  resolvedAt DateTime? @map("resolved_at")
  metadata   Json?
  createdAt  DateTime  @default(now()) @map("created_at")
  resolvedBy Int?      @map("resolved_by")

  @@index([type, resolved])
  @@index([severity, createdAt])
  @@map("system_alerts")
}

model SystemSettings {
  id          Int      @id @default(autoincrement())
  key         String   @unique
  value       String
  description String?
  updatedAt   DateTime @updatedAt @map("updated_at")
  category    String?

  @@index([category])
  @@map("system_settings")
}

model ContentModeration {
  id               Int              @id @default(autoincrement())
  mediaId          Int              @map("media_id")
  contentCreatorId Int              @map("content_creator_id")
  reviewerId       Int?             @map("reviewer_id")
  status           ModerationStatus @default(PENDING)
  reason           String?
  flags            String[]
  notes            String?
  reviewedAt       DateTime?        @map("reviewed_at")
  appealed         Boolean          @default(false)
  appealReason     String?          @map("appeal_reason")
  appealedAt       DateTime?        @map("appealed_at")
  createdAt        DateTime         @default(now()) @map("created_at")
  updatedAt        DateTime         @updatedAt @map("updated_at")
  contentCreator   User             @relation("ContentCreator", fields: [contentCreatorId], references: [id], onDelete: Cascade)
  media            Media            @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  reviewer         User?            @relation("ModerationReviewer", fields: [reviewerId], references: [id])

  @@index([status])
  @@index([mediaId])
  @@index([createdAt])
  @@map("content_moderations")
}

model CurrencyExchange {
  id           Int      @id @default(autoincrement())
  fromCurrency Currency @map("from_currency")
  toCurrency   Currency @map("to_currency")
  rate         Float
  isActive     Boolean  @default(true) @map("is_active")
  lastUpdated  DateTime @default(now()) @map("last_updated")

  @@unique([fromCurrency, toCurrency])
  @@map("currency_exchanges")
}

model FloatAccount {
  id           Int                @id @default(autoincrement())
  currency     Currency           @unique
  balance      Float              @default(0)
  createdAt    DateTime           @default(now()) @map("created_at")
  updatedAt    DateTime           @updatedAt @map("updated_at")
  transactions FloatTransaction[]

  @@map("float_accounts")
}

model FloatTransaction {
  id             Int          @id @default(autoincrement())
  floatAccountId Int          @map("float_account_id")
  amount         Float
  currency       Currency
  type           String
  reason         String
  balanceAfter   Float        @map("balance_after")
  reference      String?      @unique
  createdAt      DateTime     @default(now()) @map("created_at")
  floatAccount   FloatAccount @relation(fields: [floatAccountId], references: [id], onDelete: Cascade)

  @@index([floatAccountId, createdAt])
  @@map("float_transactions")
}

model LiveStream {
  id              Int                 @id @default(autoincrement())
  title           String
  description     String?
  userId          Int                 @map("user_id")
  status          LiveStreamStatus    @default(SCHEDULED)
  startedAt       DateTime?           @map("started_at")
  endedAt         DateTime?           @map("ended_at")
  scheduledFor    DateTime?           @map("scheduled_for")
  streamUrl       String?             @map("stream_url")
  streamKey       String?             @unique @map("stream_key")
  thumbnailUrl    String?             @map("thumbnail_url")
  viewerCount     Int                 @default(0) @map("viewer_count")
  peakViewerCount Int                 @default(0) @map("peak_viewer_count")
  isPublic        Boolean             @default(true) @map("is_public")
  isRecorded      Boolean             @default(false) @map("is_recorded")
  recordedMediaId Int?                @map("recorded_media_id")
  createdAt       DateTime            @default(now()) @map("created_at")
  updatedAt       DateTime            @updatedAt @map("updated_at")
  sessions        LiveStreamSession[]
  viewers         LiveStreamViewer[]
  user            User                @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId, status])
  @@index([status])
  @@map("live_streams")
}

model LiveStreamSession {
  id           Int         @id @default(autoincrement())
  liveStreamId Int         @map("live_stream_id")
  mediaId      Int?        @map("media_id")
  bitrate      Int?
  fps          Float?
  resolution   String?
  codec        VideoCodec?
  createdAt    DateTime    @default(now()) @map("created_at")
  liveStream   LiveStream  @relation(fields: [liveStreamId], references: [id], onDelete: Cascade)
  media        Media?      @relation(fields: [mediaId], references: [id])

  @@map("live_stream_sessions")
}

model LiveStreamViewer {
  id            Int        @id @default(autoincrement())
  liveStreamId  Int        @map("live_stream_id")
  userId        Int        @map("user_id")
  joinedAt      DateTime   @default(now()) @map("joined_at")
  leftAt        DateTime?  @map("left_at")
  watchDuration Int?       @map("watch_duration")
  liveStream    LiveStream @relation(fields: [liveStreamId], references: [id], onDelete: Cascade)
  user          User       @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([liveStreamId, userId])
  @@index([liveStreamId])
  @@map("live_stream_viewers")
}

model ProducerCollaboration {
  id              Int               @id @default(autoincrement())
  mediaId         Int               @map("media_id")
  producerId      Int               @map("producer_id")
  artistId        Int               @map("artist_id")
  type            CollaborationType
  roleDescription String?           @map("role_description")
  isConfirmed     Boolean           @default(false) @map("is_confirmed")
  confirmedAt     DateTime?         @map("confirmed_at")
  createdAt       DateTime          @default(now()) @map("created_at")
  artist          User              @relation("CollaboratingArtist", fields: [artistId], references: [id], onDelete: Cascade)
  media           Media             @relation(fields: [mediaId], references: [id], onDelete: Cascade)
  producer        User              @relation("AssignedProducer", fields: [producerId], references: [id], onDelete: Cascade)

  @@unique([mediaId, producerId, type])
  @@map("producer_collaborations")
}

model BeatPack {
  id              Int               @id @default(autoincrement())
  title           String
  description     String?           @default("")
  genre           String?
  userId          Int               @map("user_id")
  coverUrl        String?           @map("cover_url")
  price           Float             @default(0)
  accessType      MediaAccessType   @default(FREE) @map("access_type")
  playCount       Int               @default(0) @map("play_count")
  downloadCount   Int               @default(0) @map("download_count")
  likeCount       Int               @default(0) @map("like_count")
  createdAt       DateTime          @default(now()) @map("created_at")
  updatedAt       DateTime          @updatedAt @map("updated_at")
  user            User              @relation("BeatPacks", fields: [userId], references: [id], onDelete: Cascade)
  beatPackBeats   BeatPackBeat[]

  @@index([userId])
  @@index([accessType])
  @@index([createdAt])
  @@map("beat_packs")
}

model BeatPackBeat {
  id        Int      @id @default(autoincrement())
  packId    Int      @map("pack_id")
  beatId    Int      @map("beat_id")
  order     Int      @default(0)
  addedAt   DateTime @default(now()) @map("added_at")
  pack      BeatPack @relation(fields: [packId], references: [id], onDelete: Cascade)
  beat      Media    @relation("BeatPackBeats", fields: [beatId], references: [id], onDelete: Cascade)

  @@unique([packId, beatId])
  @@index([packId])
  @@map("beat_pack_beats")
}

model Resource {
  id              Int             @id @default(autoincrement())
  title           String
  description     String?         @default("")
  resourceType    ResourceType
  genre           String?
  userId          Int             @map("user_id")
  fileUrl         String?         @map("file_url")
  thumbnailUrl    String?         @map("thumbnail_url")
  price           Float           @default(0)
  accessType      MediaAccessType @default(FREE) @map("access_type")
  playCount       Int             @default(0) @map("play_count")
  downloadCount   Int             @default(0) @map("download_count")
  likeCount       Int             @default(0) @map("like_count")
  createdAt       DateTime        @default(now()) @map("created_at")
  updatedAt       DateTime        @updatedAt @map("updated_at")
  user            User            @relation("Resources", fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([resourceType])
  @@index([accessType])
  @@index([createdAt])
  @@map("resources")
}

model sessions {
  id         Int      @id @default(autoincrement())
  user_id    Int?     @map("user_id")
  token      String   @unique
  ip_address String?  @map("ip_address")
  user_agent String?  @map("user_agent")
  expires_at DateTime @map("expires_at")
  created_at DateTime @default(now()) @map("created_at")

  @@map("sessions_legacy")
}

enum MediaType {
  AUDIO
  VIDEO
  PODCAST
  LIVE_STREAM
  ALBUM
  COLLECTION
  PLAYLIST
}

enum VideoQuality {
  MOBILE
  SD
  HD
  FULL_HD
  QUAD_HD
  ULTRA_HD
  ULTRA_HD_8K
}

enum VideoCodec {
  H264
  H265_HEVC
  VP9
  AV1
}

enum AudioCodec {
  AAC
  MP3
  OPUS
  FLAC
  ALAC
  VORBIS
}

enum PlaylistType {
  SYSTEM
  USER
  SMART
  RADIO
  COLLABORATIVE
  ALGORITHM_GENERATED
  TRENDING
  CURATED
}

enum UserRole {
  USER
  ARTIST
  ADMIN
  MODERATOR
  RESELLER
  PRODUCER
  CONTENT_MANAGER
  ANALYTICS_VIEWER
}

enum TransactionStatus {
  PENDING
  COMPLETED
  FAILED
  REFUNDED
  PARTIALLY_REFUNDED
  PROCESSING
  DISPUTED
  CANCELLED
}

enum MediaAccessType {
  FREE
  PREMIUM
  PAY_PER_VIEW
  SUBSCRIPTION
  TIME_LIMITED
  EXCLUSIVE
}

enum PaymentMethod {
  MOBILE_MONEY
  BANK_TRANSFER
  CREDIT_CARD
  CRYPTO
  OTHER
  DEBIT_CARD
  WALLET
}

enum PaymentProvider {
  MTN_MONEY
  AIRTEL_MONEY
  STRIPE
  PAYPAL
  FLUTTERWAVE
  PAYSTACK
  MPESA
  BANK
  OTHER
  COINBASE
  CRYPTO_GATEWAY
}

enum Currency {
  USD
  EUR
  GBP
  ZMW
  ZAR
  KES
  NGN
  GHS
  UGX
  TZS
  XOF
  XAF
  AUD
  CAD
  JPY
  CNY
  INR
  BRL
  MXN
  BWP
  RWF
  ETB
  MUR
  SCR
}

enum SubscriptionPlan {
  DAILY
  WEEKLY
  MONTHLY
  YEARLY
  LIFETIME
}

enum SubscriptionStatus {
  ACTIVE
  PAST_DUE
  CANCELED
  EXPIRED
  SUSPENDED
  PENDING
}

enum CommissionStatus {
  PENDING
  PAID
  FAILED
  HOLD
  DISPUTED
  REFUNDED
}

enum ResellerLinkStatus {
  ACTIVE
  INACTIVE
  SUSPENDED
  EXPIRED
  ARCHIVED
}

enum DeviceRestrictionLevel {
  NONE
  BASIC
  STRICT
  ENCRYPTED
  ULTRA_SECURE
}

enum DownloadAccessType {
  OFFLINE
  ONLINE
  STREAMING
  BOTH
}

enum UserStatus {
  PENDING
  ACTIVE
  SUSPENDED
  VERIFIED
  REJECTED
  DELETED_PENDING
  DELETED_COMPLETED
}

enum KYCStatus {
  NOT_SUBMITTED
  PENDING_REVIEW
  APPROVED
  REJECTED
  ADDITIONAL_INFO_NEEDED
  EXPIRED
}

enum DocumentType {
  NATIONAL_ID
  PASSPORT
  DRIVERS_LICENSE
  BUSINESS_REGISTRATION
  TAX_CERTIFICATE
  UTILITY_BILL
  BANK_STATEMENT
  PROOF_OF_ADDRESS
}

enum VerificationMethod {
  EMAIL
  PHONE
  DOCUMENT
  BIOMETRIC
  SMS
  TWO_FACTOR_AUTH
}

enum ContentStatus {
  DRAFT
  SUBMITTED
  APPROVED
  PUBLISHED
  REJECTED
  ARCHIVED
  DELETED
  SCHEDULED
  PRIVATE
}

enum DeletionReason {
  USER_REQUEST
  DUPLICATE
  COPYRIGHT_VIOLATION
  POLICY_VIOLATION
  ACCOUNT_DELETION
  ADMIN_REMOVAL
  EXPIRED
  OTHER
}

enum ModerationStatus {
  PENDING
  APPROVED
  REJECTED
  FLAGGED
  UNDER_REVIEW
  APPEALED
}

enum LiveStreamStatus {
  SCHEDULED
  LIVE
  PAUSED
  ENDED
  CANCELLED
  PROCESSING
  ARCHIVED
}

enum RecommendationType {
  TRENDING
  PERSONALIZED
  COLLABORATIVE
  CONTENT_BASED
  SOCIAL
  TRENDING_LOCAL
  TRENDING_GLOBAL
  SIMILAR_ARTIST
}

enum StreamingQualityPreset {
  ADAPTIVE
  MOBILE_FIRST
  BALANCED
  HIGH_QUALITY
  DATA_SAVER
}

enum NotificationType {
  NEW_RELEASE
  FOLLOW
  LIKE
  COMMENT
  SHARE
  PURCHASE
  DOWNLOAD_COMPLETE
  STREAM_MILESTONE
  SYSTEM
  PROMOTION
  LIVE_STREAM_START
  COLLABORATION_REQUEST
}

enum AudioFeatureCategory {
  ENERGETIC
  ACOUSTIC
  DANCEABLE
  MELANCHOLIC
  UPLIFTING
  DARK
  RELAXED
  AGGRESSIVE
  INSTRUMENTAL
  VOCAL_HEAVY
}

enum CollaborationType {
  PRODUCER
  FEATURE_ARTIST
  SONGWRITER
  ENGINEER
  MIXER
  MASTERING
  BAND_MEMBER
  GUEST_ARTIST
}

enum ResourceType {
  SAMPLE
  LOOP
  TEMPLATE
  PRESET
  SOUND
}

enum RatingValue {
  ONE
  TWO
  THREE
  FOUR
  FIVE
}
